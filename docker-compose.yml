services:
  bot:
    build: .
    container_name: pilyuli_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/app/data
    environment:
      - DB_PATH=/app/data/pill_bot.db
